/* #include "" */

// extern void json_to_binary(const unsigned char* s, int len);

int twice(int x) {
  return 2 * x;
}

int sum(int x, int y) {
  return x + y;
}

int main()
{
  erl_init(NULL,0);
  
  return 0;
}